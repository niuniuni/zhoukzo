var Upload = function(el, ops) {
    this.addbox = document.querySelector(el);

    this.t = ops.type
    this.s = ops.size;
    this.init;
}
Upload.prototype = {
    constructor: Upload,
    init: function() {
        var that = this;

    }
}